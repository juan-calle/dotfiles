[core]
  excludesfile = /Users/jc/.gitignore_global
  editor = code -w
  autocrlf = false
[user]
	name = Juan Calle
	email = 8818957+juan-calle@users.noreply.github.com
  username = juan-calle
	signingkey = D3C8FEB68B6351F4
[init]
  defaultBranch = main
[github]
  user = juan-calle
[push]
  default = simple
[credential]
  helper = osxkeychain
[filter "lfs"]
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
[alias]
  co = checkout
  conf = config --global -e
  up = !git pull --rebase --prune $@ && git submodule update --init --recursive
  cob = checkout -b
  cm = !git add -A && git commit -m
  save = !git add -A && git commit -m 'SAVEPOINT'
  wip = !git add -u && git commit -m "WIP"
  undo = reset HEAD~1 --mixed
  amend = commit -a --amend
  wipe = !git add -A && git commit -qm 'WIPE SAVEPOINT' && git reset HEAD~1 --hard
  default = !git symbolic-ref refs/remotes/origin/HEAD | sed 's@^refs/remotes/origin/@@'
  bdone = "!f() { DEFAULT=$(git default); git checkout ${1-$DEFAULT} && git up && git bclean ${1-$DEFAULT}; }; f"
  bclean = "!f() { DEFAULT=$(git default); git branch --merged ${1-$DEFAULT} | grep -v " ${1-$DEFAULT}$" | xargs git branch -d; }; f"
  cdef = "!git checkout $(git originhead)"
  originhead = "!git remote show origin | grep 'HEAD branch' | cut -d ' ' -f5"
  la = "!git config -l | grep alias | cut -c 7-"
  lg = !git log --pretty=format:\"%C(magenta)%h%Creset -%C(red)%d%Creset %s %C(dim green)(%cr) [%an]\" --abbrev-commit -30
  fixsymbolicrefs = !git symbolic-ref refs/remotes/origin/HEAD refs/remotes/origin/main
  forcelease = !git push --force-with-lease
  rename = "!moveit() { git push origin --delete `git branch --show-current` || true; git branch -m $1; git push --set-upstream origin $1; }; moveit"
  remotedelete = "push --delete origin"
  # reporoot = !basename `git rev-parse --show-toplevel`
  # create = "!f() { DEFAULT=$(!basename `git rev-parse --show-toplevel`); git init && gh repo create $1 --private && git remote add origin https://github.com/juan-calle/${1-$DEFAULT}.git && git branch -M main; }; f"
  # pushup = !git push -u origin main
  # mirror = "!f() { DEFAULT=$(!basename `git rev-parse --show-toplevel`); gh repo create ${1-$DEFAULT} --private && git remote remove origin && git remote add origin https://github.com/juan-calle/${1-$DEFAULT}.git && git branch -M main && git pushup; }; f"
  # ps = !git push origin HEAD
  # nrepo = !git create $1 && git addremote
  # syncmain = !git branch --set-upstream-to=origin/main main
  # delete = !git remote remove origin && gh repo delete juan-calle/$(git root)
[gpg]
	program = gpg
[commit]
	gpgsign = true
[tag]
  forceSignAnnotated = false
